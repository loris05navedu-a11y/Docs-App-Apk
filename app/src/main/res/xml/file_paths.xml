<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Seuls ces dossiers sont exposés : les fichiers que l'app vient de
         produire, à l'export ou à la conversion. -->
    <cache-path name="partage" path="partage/" />
    <files-path name="conversions" path="conversions/" />
</paths>
